apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: bigsi-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: bigsi
          servicePort: 80